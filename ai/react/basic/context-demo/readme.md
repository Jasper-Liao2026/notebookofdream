# useContext
上下文？
## 组件通信

- props 传递数据 自定义事件
共享数据 统一数据状态管理

- 父子组件通信
- 兄弟关系
- 爷孙关系
    原来的那套 层层传递 层次太深，搬运工作就太麻烦
- 陌生人关系

## 组件层次比较深
- useContext 上下文
- 提供数据上下文 最外层
Context.Provider
- 消费上下文
跨越层级，不考虑层级
链接上下文，消费数据

## 步骤
1.createContext 创建上下文
2.Provider 组件包裹组件树
3.useContext 消费上下文

## 自定义hooks
- use开头的函数
- hooks目录下，属于架构
- 封装了响应式、附作用等，复用

## 监听鼠标移动事件，鼠标坐标显示在页面上
- 抽象
    响应式的鼠标坐标

- 封装
    hooks 目录下