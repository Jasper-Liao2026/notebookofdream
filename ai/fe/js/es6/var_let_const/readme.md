# es6

JavaScript 蹭了一波java热度，弱类型动态语言，早期设计用来给网页添加交互（例如：幻灯片），DOM 编程 
JS 是以Escript 为语言标准的语言

ES6 是JS的新版本，ES5，ES6+  2015年，企业级大型项目开发发展
JS 是一个KPI项目 一周就开发出来了
## 申明变量并赋值
- var es5 现在不用
- let es6+ 代替var

## 作用域 scope
### 作用域嵌套
- 全局作用域
- 函数局部作用域
    - 局部作用域
- 块级作用域 {  }
变量属于作用域，变量声明JS为弱类型，类型由值决定
- 查找变量的规则
    - 现在当前作用域查找
        找到了，直接拿到
        如果没找到，向外层作用域查找
    冒泡查找
    - 当在全局作用域都没有找到，停下来报错

- 函数/代码块运行之后，垃圾回收了
    - 内存角度 变量的声明
    在内存中申请了一块区域
    结束之后销毁函数，回收内存
    变量的生命周期

- Assingnment to constant variable
- ReferenceError: XXX is not difined

##  var let const
早期的js 使用var 声明变量，没有常量，用代码规范约束
js 设计比较赶工 浏览器的副产品
js 没有经过深思熟虑，有一些瑕疵
let 是变量 const 是常量